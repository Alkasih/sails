// Connect to a server on a different domain/subdomain).

// Disable autoConnect.
// io.sails.autoConnect = false;

//Cross-domain.
// .config(function($sailsConnection) {
//   $sailsConnection.autoConnect = false;
// 	$sailsConnection.baseUrl = 'http://localhost:1337';
// })

io.sails.url = 'http://localhost:1337';
